# Template — replace base64 values before apply. Do not commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: findoc-secrets
  namespace: findoc-analyzer
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
  API_KEYS: "changeme-prod-key"
  WEBHOOK_SIGNING_SECRET: "changeme-hmac"
