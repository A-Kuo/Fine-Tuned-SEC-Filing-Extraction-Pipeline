# Inference-container dependencies for sagemaker/inference.py.
# Mirrors the subset of the root requirements.txt actually needed to load
# FinancialLLM and run generation -- not the full training/serving/eval
# stack (no mlflow, no fastapi/uvicorn, no dagshub).
torch>=2.0.0
transformers>=4.35.0
peft>=0.7.0
bitsandbytes>=0.41.0
accelerate>=0.25.0
sentencepiece>=0.1.99
protobuf>=3.20.0
loguru>=0.7.0
