# GitHub Actions: test/lint dependencies only (no torch / vLLM / GPU).
# Keeps CI fast (~1–2 min) while matching imports used by pytest.
#
# Install: pip install -r requirements-ci.txt

pyyaml>=6.0
loguru>=0.7.0
python-dotenv>=1.0.0

# API + rate limiting + metrics (serving/api.py)
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
pydantic>=2.0
slowapi>=0.1.9
limits>=3.6.0
prometheus-client>=0.19.0

# DB layer imports (lazy connect; tests use mocks)
psycopg2-binary>=2.9.9
redis>=5.0.0

# Monitoring dashboard imports (if pulled transitively)
streamlit>=1.29.0
streamlit-autorefresh>=1.0.1

# Evaluation / monitoring
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
jsonschema>=4.20.0

# Dev / test
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
rich>=13.7.0
tqdm>=4.66.0
click>=8.1.0
